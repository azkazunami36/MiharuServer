import { headerSetting } from "../src/js/headerSetting.js";

addEventListener("load", async () => {
    headerSetting();
    const novelNames: {
        name: string;
    }[] = [
        {
            name: "男の僕と女の僕。記憶は同じ！？"
        }
    ]
})